package com.yixin.star.service;

import com.yixin.star.domain.SystemConfig;

import java.util.List;

public interface SystemConfigService{

    List<SystemConfig> findAllSystem();
    int insert(SystemConfig systemConfig);

    int insertSelective(SystemConfig systemConfig);

    int insertList(List<SystemConfig> systemConfigs);

    int update(SystemConfig systemConfig);

    List<SystemConfig> findBySystem(String system);
}
